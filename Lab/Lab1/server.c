#include "server.h"
#include "parser.h"
int main()
{
    // test parser
//    struct Message message;
//    const char *requestStr = "|0|Done|";
//    int type = ParseRequest(requestStr, &message);
//    printf("%d", type);
    StartServer();
    return 0;
}